[package]
name = "neojava"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
chrono = "0.4"
regex = "1.10"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
